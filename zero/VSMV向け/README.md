## VSMV向け
VSMVのビルドするとき向けのものです
## なぜ分ける必要があるのですか
イントラインassembly構文が違うからです
## VSMVのイントライン
```cpp
    while(1) {
        // MSVC（32bit）向けのインラインアセンブリ構文
        __asm { hlt }
    }
  ```
## ほかのイントライン
```cpp
 while(1) {
        __asm__("hlt");
    }
```
---
